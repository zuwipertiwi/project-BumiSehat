<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Masuk - BumiSehat 🌿</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }

    .hero-gradient {
      background: linear-gradient(135deg, #064e3b 0%, #059669 25%, #34d399 50%, #6ee7b7 75%, #a7f3d0 100%);
      background-size: 400% 400%;
      animation: gradientShift 8s ease infinite;
    }

    @keyframes gradientShift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.3);
      box-shadow: 0 25px 60px rgba(0, 0, 0, 0.12);
    }

    .input-group {
      position: relative;
      margin-bottom: 1.5rem;
    }

    .input-field {
      width: 100%;
      padding: 1rem 1rem 1rem 3.5rem;
      border: 2px solid #e5e7eb;
      border-radius: 12px;
      outline: none;
      transition: all 0.3s ease;
      background: rgba(255, 255, 255, 0.9);
      font-size: 16px;
    }

    .input-field:focus {
      border-color: #10b981;
      box-shadow: 0 0 0 4px rgba(16, 185, 129, 0.1);
      background: white;
      transform: translateY(-2px);
    }

    .input-icon {
      position: absolute;
      left: 1rem;
      top: 50%;
      transform: translateY(-50%);
      color: #6b7280;
      font-size: 1.25rem;
      transition: all 0.3s ease;
    }

    .input-field:focus + .input-icon {
      color: #10b981;
      transform: translateY(-50%) scale(1.1);
    }

    .login-btn {
      background: linear-gradient(135deg, #10b981, #34d399);
      box-shadow: 0 10px 30px rgba(16, 185, 129, 0.3);
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .login-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 15px 40px rgba(16, 185, 129, 0.4);
    }

    .login-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
      transition: left 0.5s;
    }

    .login-btn:hover::before {
      left: 100%;
    }

    .floating-element {
      position: absolute;
      opacity: 0.6;
      animation: float 8s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px) rotate(0deg); }
      33% { transform: translateY(-30px) rotate(120deg); }
      66% { transform: translateY(-15px) rotate(240deg); }
    }

    .fade-in {
      animation: fadeIn 0.8s ease-out forwards;
      opacity: 0;
      transform: translateY(30px);
    }

    .fade-in-delay {
      animation: fadeIn 0.8s ease-out 0.3s forwards;
      opacity: 0;
      transform: translateY(30px);
    }

    .fade-in-delay-2 {
      animation: fadeIn 0.8s ease-out 0.6s forwards;
      opacity: 0;
      transform: translateY(30px);
    }

    @keyframes fadeIn {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .remember-checkbox {
      appearance: none;
      width: 1.125rem;
      height: 1.125rem;
      border: 2px solid #d1d5db;
      border-radius: 4px;
      position: relative;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .remember-checkbox:checked {
      background: #10b981;
      border-color: #10b981;
    }

    .remember-checkbox:checked::after {
      content: '✓';
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      color: white;
      font-size: 0.75rem;
      font-weight: bold;
    }

    .welcome-message {
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(52, 211, 153, 0.1));
      border-left: 4px solid #10b981;
    }

    .social-btn {
      transition: all 0.3s ease;
      border: 2px solid #e5e7eb;
    }

    .social-btn:hover {
      border-color: #10b981;
      background: rgba(16, 185, 129, 0.05);
      transform: translateY(-2px);
    }

    .leaf-decoration {
      background-image: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%2334d399' fill-opacity='0.05'%3E%3Cpath d='M20 20c0-5.5-4.5-10-10-10s-10 4.5-10 10 4.5 10 10 10 10-4.5 10-10zm0 0c0-5.5 4.5-10 10-10s10 4.5 10 10-4.5 10-10 10-10-4.5-10-10z'/%3E%3C/g%3E%3C/svg%3E");
    }
  </style>
</head>
<body class="hero-gradient min-h-screen flex items-center justify-center p-4 relative overflow-hidden leaf-decoration">

  <!-- Floating Elements -->
  <div class="floating-element top-16 left-8" style="animation-delay: 0s;">
    <span class="text-6xl opacity-20">🌿</span>
  </div>
  <div class="floating-element top-32 right-16" style="animation-delay: 3s;">
    <span class="text-4xl opacity-20">🌱</span>
  </div>
  <div class="floating-element bottom-24 left-16" style="animation-delay: 6s;">
    <span class="text-5xl opacity-20">🍃</span>
  </div>
  <div class="floating-element top-1/3 right-8" style="animation-delay: 2s;">
    <span class="text-3xl opacity-20">🌾</span>
  </div>
  <div class="floating-element bottom-1/3 left-1/4" style="animation-delay: 4s;">
    <span class="text-4xl opacity-20">🌳</span>
  </div>

  <!-- Main Content -->
  <div class="w-full max-w-5xl mx-auto grid md:grid-cols-2 gap-8 items-center">

    <!-- Left Side - Welcome Message -->
    <div class="hidden md:block fade-in">
      <div class="text-white space-y-6">
        <div class="inline-flex items-center space-x-3 mb-8">
          <div class="w-12 h-12 bg-white rounded-full flex items-center justify-center">
            <span class="text-green-600 text-2xl">🌱</span>
          </div>
          <span class="text-2xl font-bold">BumiSehat</span>
        </div>

        <h1 class="text-4xl md:text-5xl font-bold leading-tight">
          Selamat Datang Kembali!
        </h1>

        <p class="text-xl text-white/90 leading-relaxed">
          Lanjutkan perjalanan ramah lingkungan mu. Mari bersama-sama menciptakan dampak positif untuk bumi kita.
        </p>

        <div class="welcome-message p-6 rounded-2xl">
          <h3 class="text-lg font-semibold text-white mb-3">✨ Yang Baru Hari Ini:</h3>
          <ul class="space-y-2 text-white/90">
            <li class="flex items-center space-x-2">
              <span class="text-green-300">🎯</span>
              <span>Target baru: Kurangi sampah plastik 50%</span>
            </li>
            <li class="flex items-center space-x-2">
              <span class="text-green-300">🏆</span>
              <span>Badge "Eco Warrior" telah tersedia!</span>
            </li>
            <li class="flex items-center space-x-2">
              <span class="text-green-300">📊</span>
              <span>Fitur statistik yang lebih detail</span>
            </li>
          </ul>
        </div>

        <div class="flex items-center space-x-6 text-sm text-white/80">
          <div class="flex items-center space-x-2">
            <span class="text-2xl">👥</span>
            <span>2.5K+ Pengguna Aktif</span>
          </div>
          <div class="flex items-center space-x-2">
            <span class="text-2xl">🌍</span>
            <span>10K+ Aksi Hijau</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Right Side - Login Form -->
    <div class="glass-card rounded-3xl p-8 w-full max-w-md mx-auto relative z-10 fade-in-delay">

      <!-- Header -->
      <div class="text-center mb-8">
        <div class="w-16 h-16 bg-gradient-to-br from-green-400 to-emerald-500 rounded-full flex items-center justify-center mx-auto mb-4 shadow-lg">
          <span class="text-2xl">🔐</span>
        </div>
        <h2 class="text-2xl font-bold text-gray-800 mb-2">Masuk ke Akun Anda</h2>
        <p class="text-gray-600">Lanjutkan misi hijau Anda</p>
      </div>

      <!-- Pesan Error / Success -->
    @if (session.get('error'))
    <div class="mb-6 p-4 rounded bg-red-100 text-red-700 border border-red-400">
      {{ session.get('error') }}
    </div>
    @endif

    @if (session.get('success'))
    <div class="mb-6 p-4 rounded bg-green-100 text-green-700 border border-green-400">
      {{ session.get('success') }}
    </div>
    @endif

    <!-- Pesan Error / Success -->
@if (flashMessages.get('error'))
<div class="mb-6 p-4 rounded bg-red-100 text-red-700 border border-red-400">
  {{ flashMessages.get('error') }}
</div>
@endif

@if (flashMessages.get('success'))
<div class="mb-6 p-4 rounded bg-green-100 text-green-700 border border-green-400">
  {{ flashMessages.get('success') }}
</div>
@endif



      <!-- Login Form -->
      <form action="/login" method="post" class="space-y-6">

        <!-- Email Field -->
        <div class="input-group">
          <input
            type="email"
            name="email"
            id="email"
            required
            class="input-field"
            placeholder="Alamat Email"
            autocomplete="email"
          />
          <span class="input-icon">📧</span>
        </div>

        <!-- Password Field -->
        <div class="input-group">
          <input
            type="password"
            name="password"
            id="password"
            required
            class="input-field"
            placeholder="Kata Sandi"
            autocomplete="current-password"
          />
          <span class="input-icon">🔒</span>
        </div>

        <!-- Remember Me & Forgot Password -->
        <div class="flex items-center justify-between">
          <label class="flex items-center space-x-2 cursor-pointer">
            <input type="checkbox" name="remember" class="remember-checkbox">
            <span class="text-sm text-gray-600">Ingat saya</span>
          </label>
          <a href="#" class="text-sm text-green-600 hover:text-green-700 hover:underline transition-colors">
            Lupa kata sandi?
          </a>
        </div>

        <!-- Login Button -->
        <button type="submit" class="login-btn w-full text-white font-semibold py-4 rounded-xl relative">
          <span class="relative z-10">🚀 Masuk Sekarang</span>
        </button>
      </form>

      <!-- Divider -->
      <div class="flex items-center my-6">
        <div class="flex-1 border-t border-gray-200"></div>
        <span class="px-4 text-sm text-gray-500">atau</span>
        <div class="flex-1 border-t border-gray-200"></div>
      </div>

      <!-- Social Login -->
      <div class="space-y-3">
        <button class="social-btn w-full flex items-center justify-center space-x-3 py-3 rounded-xl font-medium text-gray-700 hover:text-gray-900">
          <span class="text-xl">🌐</span>
          <span>Masuk dengan Google</span>
        </button>
      </div>

      <!-- Register Link -->
      <div class="text-center mt-8 pt-6 border-t border-gray-200">
        <p class="text-sm text-gray-600">
          Belum punya akun?
          <a href="/register" class="text-green-600 hover:text-green-700 font-semibold hover:underline transition-colors ml-1">
            Daftar gratis
          </a>
        </p>
      </div>

      <!-- Quick Stats -->
      <div class="mt-6 grid grid-cols-2 gap-4 text-center">
        <div class="bg-green-50 rounded-xl p-3">
          <div class="text-2xl font-bold text-green-600">2.5K+</div>
          <div class="text-xs text-gray-600">Pengguna Aktif</div>
        </div>
        <div class="bg-blue-50 rounded-xl p-3">
          <div class="text-2xl font-bold text-blue-600">10K+</div>
          <div class="text-xs text-gray-600">Aksi Hijau</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Mobile Welcome Message -->
  <div class="md:hidden fixed bottom-4 left-4 right-4 fade-in-delay-2">
    <div class="welcome-message p-4 rounded-2xl text-white">
      <div class="flex items-center space-x-2 mb-2">
        <span class="text-xl">✨</span>
        <span class="font-semibold">Tips Hari Ini</span>
      </div>
      <p class="text-sm text-white/90">
        Bawa botol minum sendiri dan kurangi sampah plastik hingga 50% per hari!
      </p>
    </div>
  </div>

  <script>
    // Add form validation and interactive elements
    document.addEventListener('DOMContentLoaded', function() {
      const form = document.querySelector('form');
      const emailInput = document.getElementById('email');
      const passwordInput = document.getElementById('password');

      // Email validation
      emailInput.addEventListener('blur', function() {
        const email = this.value;
        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

        if (email && !emailRegex.test(email)) {
          this.style.borderColor = '#ef4444';
        } else if (email) {
          this.style.borderColor = '#10b981';
        }
      });

      // Password strength indicator
      passwordInput.addEventListener('input', function() {
        const password = this.value;

        if (password.length >= 6) {
          this.style.borderColor = '#10b981';
        } else if (password.length > 0) {
          this.style.borderColor = '#f59e0b';
        }
      });

      // Form submission
      form.addEventListener('submit', function(e) {
        const submitBtn = this.querySelector('button[type="submit"]');
        const originalText = submitBtn.querySelector('span').textContent;

        submitBtn.querySelector('span').innerHTML = '⏳ Sedang masuk...';
        submitBtn.disabled = true;

        // Reset after 3 seconds (remove this in production)
        setTimeout(() => {
          submitBtn.querySelector('span').textContent = originalText;
          submitBtn.disabled = false;
        }, 3000);
      });
    });
  </script>
</body>
</html>
