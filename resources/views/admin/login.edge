<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Login - BumiSehat 🌿</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }

    .admin-gradient {
      background: linear-gradient(135deg, #1e3a8a 0%, #3730a3 25%, #4338ca 50%, #6366f1 75%, #8b5cf6 100%);
      background-size: 400% 400%;
      animation: gradientShift 10s ease infinite;
    }
    
    @keyframes gradientShift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.3);
      box-shadow: 0 25px 60px rgba(0, 0, 0, 0.15);
    }

    .input-group {
      position: relative;
      margin-bottom: 1.5rem;
    }

    .input-field {
      width: 100%;
      padding: 1rem 1rem 1rem 3.5rem;
      border: 2px solid #e5e7eb;
      border-radius: 12px;
      outline: none;
      transition: all 0.3s ease;
      background: rgba(255, 255, 255, 0.9);
      font-size: 16px;
    }

    .input-field:focus {
      border-color: #4338ca;
      box-shadow: 0 0 0 4px rgba(67, 56, 202, 0.1);
      background: white;
      transform: translateY(-2px);
    }

    .input-icon {
      position: absolute;
      left: 1rem;
      top: 50%;
      transform: translateY(-50%);
      color: #6b7280;
      font-size: 1.25rem;
      transition: all 0.3s ease;
    }

    .input-field:focus + .input-icon {
      color: #4338ca;
      transform: translateY(-50%) scale(1.1);
    }

    .admin-btn {
      background: linear-gradient(135deg, #4338ca, #6366f1);
      box-shadow: 0 10px 30px rgba(67, 56, 202, 0.3);
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .admin-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 15px 40px rgba(67, 56, 202, 0.4);
    }

    .admin-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
      transition: left 0.5s;
    }

    .admin-btn:hover::before {
      left: 100%;
    }

    .floating-element {
      position: absolute;
      opacity: 0.4;
      animation: float 10s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px) rotate(0deg); }
      33% { transform: translateY(-40px) rotate(120deg); }
      66% { transform: translateY(-20px) rotate(240deg); }
    }

    .fade-in {
      animation: fadeIn 0.8s ease-out forwards;
      opacity: 0;
      transform: translateY(30px);
    }

    .fade-in-delay {
      animation: fadeIn 0.8s ease-out 0.3s forwards;
      opacity: 0;
      transform: translateY(30px);
    }

    .fade-in-delay-2 {
      animation: fadeIn 0.8s ease-out 0.6s forwards;
      opacity: 0;
      transform: translateY(30px);
    }

    @keyframes fadeIn {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .remember-checkbox {
      appearance: none;
      width: 1.125rem;
      height: 1.125rem;
      border: 2px solid #d1d5db;
      border-radius: 4px;
      position: relative;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .remember-checkbox:checked {
      background: #4338ca;
      border-color: #4338ca;
    }

    .remember-checkbox:checked::after {
      content: '✓';
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      color: white;
      font-size: 0.75rem;
      font-weight: bold;
    }

    .admin-welcome {
      background: linear-gradient(135deg, rgba(67, 56, 202, 0.1), rgba(99, 102, 241, 0.1));
      border-left: 4px solid #4338ca;
    }

    .security-badge {
      background: linear-gradient(135deg, #dc2626, #ef4444);
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.8; }
    }

    .admin-stats {
      background: rgba(67, 56, 202, 0.05);
      border: 1px solid rgba(67, 56, 202, 0.1);
    }

    .warning-box {
      background: linear-gradient(135deg, rgba(245, 158, 11, 0.1), rgba(251, 191, 36, 0.1));
      border: 1px solid rgba(245, 158, 11, 0.3);
    }

    .pattern-overlay {
      background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%234338ca' fill-opacity='0.05'%3E%3Cpath d='M30 30c0-8.3-6.7-15-15-15s-15 6.7-15 15 6.7 15 15 15 15-6.7 15-15zm0 0c0-8.3 6.7-15 15-15s15 6.7 15 15-6.7 15-15 15-15-6.7-15-15z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    }
  </style>
</head>
<body class="admin-gradient min-h-screen flex items-center justify-center p-4 relative overflow-hidden pattern-overlay">

  <!-- Floating Elements -->
  <div class="floating-element top-20 left-12" style="animation-delay: 0s;">
    <span class="text-6xl opacity-30">🛡️</span>
  </div>
  <div class="floating-element top-40 right-20" style="animation-delay: 4s;">
    <span class="text-4xl opacity-30">⚙️</span>
  </div>
  <div class="floating-element bottom-32 left-20" style="animation-delay: 8s;">
    <span class="text-5xl opacity-30">📊</span>
  </div>
  <div class="floating-element top-1/4 right-12" style="animation-delay: 2s;">
    <span class="text-3xl opacity-30">🔐</span>
  </div>
  <div class="floating-element bottom-1/4 left-1/3" style="animation-delay: 6s;">
    <span class="text-4xl opacity-30">👨‍💼</span>
  </div>

  <!-- Main Content -->
  <div class="w-full max-w-6xl mx-auto grid lg:grid-cols-2 gap-12 items-center">
    
    <!-- Left Side - Admin Welcome -->
    <div class="hidden lg:block fade-in">
      <div class="text-white space-y-8">
        <div class="inline-flex items-center space-x-4 mb-8">
          <div class="w-14 h-14 bg-white rounded-full flex items-center justify-center shadow-lg">
            <span class="text-indigo-600 text-2xl">🛡️</span>
          </div>
          <div>
            <span class="text-3xl font-bold">BumiSehat</span>
            <div class="text-sm bg-red-500 text-white px-2 py-1 rounded-full inline-block ml-2">
              ADMIN
            </div>
          </div>
        </div>
        
        <h1 class="text-5xl md:text-6xl font-bold leading-tight">
          Panel Administrator
        </h1>
        
        <p class="text-xl text-white/90 leading-relaxed">
          Akses sistem manajemen BumiSehat. Kelola pengguna, konten, dan monitoring platform dengan keamanan tingkat tinggi.
        </p>
        
        <div class="admin-welcome p-6 rounded-2xl">
          <div class="flex items-center space-x-3 mb-4">
            <span class="text-2xl">📋</span>
            <h3 class="text-lg font-semibold text-white">Dashboard Overview</h3>
          </div>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div class="admin-stats p-4 rounded-xl">
              <div class="text-2xl font-bold text-white">2,547</div>
              <div class="text-sm text-white/80">Total Users</div>
            </div>
            <div class="admin-stats p-4 rounded-xl">
              <div class="text-2xl font-bold text-white">10,234</div>
              <div class="text-sm text-white/80">Green Actions</div>
            </div>
            <div class="admin-stats p-4 rounded-xl">
              <div class="text-2xl font-bold text-white">98.5%</div>
              <div class="text-sm text-white/80">System Uptime</div>
            </div>
            <div class="admin-stats p-4 rounded-xl">
              <div class="text-2xl font-bold text-white">45</div>
              <div class="text-sm text-white/80">Active Reports</div>
            </div>
          </div>
        </div>
        
        <div class="warning-box p-6 rounded-2xl">
          <div class="flex items-start space-x-3">
            <span class="text-2xl">⚠️</span>
            <div>
              <h4 class="font-semibold text-white mb-2">Keamanan Sistem</h4>
              <p class="text-sm text-white/90">
                Pastikan Anda login dari perangkat yang aman. Jangan bagikan kredensial admin kepada siapa pun.
              </p>
            </div>
          </div>
        </div>
        
        <div class="flex items-center space-x-8 text-sm text-white/80">
          <div class="flex items-center space-x-2">
            <span class="text-2xl">🔒</span>
            <span>SSL Secured</span>
          </div>
          <div class="flex items-center space-x-2">
            <span class="text-2xl">🛡️</span>
            <span>2FA Protected</span>
          </div>
          <div class="flex items-center space-x-2">
            <span class="text-2xl">📝</span>
            <span>Activity Logged</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Right Side - Admin Login Form -->
    <div class="glass-card rounded-3xl p-10 w-full max-w-lg mx-auto relative z-10 fade-in-delay">
      
      <!-- Security Badge -->
      <div class="absolute -top-4 -right-4">
        <div class="security-badge text-white px-4 py-2 rounded-full text-sm font-bold shadow-lg">
          🔐 SECURE ACCESS
        </div>
      </div>
      
      <!-- Header -->
      <div class="text-center mb-10">
        <div class="w-20 h-20 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-full flex items-center justify-center mx-auto mb-6 shadow-xl">
          <span class="text-3xl">👨‍💼</span>
        </div>
        <h2 class="text-3xl font-bold text-gray-800 mb-3">Administrator Login</h2>
        <p class="text-gray-600">Akses panel kontrol sistem BumiSehat</p>
      </div>

      <!-- Admin Login Form -->
      <form action="/admin/login" method="post" class="space-y-6">
        
        <!-- Admin ID / Username Field -->
        <div class="input-group">
          <input
            type="text"
            name="admin_id"
            id="admin_id"
            required
            class="input-field"
            placeholder="Admin ID / Username"
            autocomplete="username"
          />
          <span class="input-icon">👤</span>
        </div>

        <!-- Password Field -->
        <div class="input-group">
          <input
            type="password"
            name="password"
            id="password"
            required
            class="input-field"
            placeholder="Password"
            autocomplete="current-password"
          />
          <span class="input-icon">🔐</span>
        </div>

        <!-- 2FA Code Field -->
        <div class="input-group">
          <input
            type="text"
            name="two_fa_code"
            id="two_fa_code"
            class="input-field"
            placeholder="2FA Code (Optional)"
            maxlength="6"
          />
          <span class="input-icon">🛡️</span>
        </div>

        <!-- Remember & Security Options -->
        <div class="space-y-4">
          <div class="flex items-center justify-between">
            <label class="flex items-center space-x-2 cursor-pointer">
              <input type="checkbox" name="remember" class="remember-checkbox">
              <span class="text-sm text-gray-600">Remember this device</span>
            </label>
            <a href="#" class="text-sm text-indigo-600 hover:text-indigo-700 hover:underline transition-colors">
              Forgot password?
            </a>
          </div>
          
          <div class="bg-amber-50 border border-amber-200 rounded-lg p-3">
            <div class="flex items-center space-x-2">
              <span class="text-amber-600">⚠️</span>
              <span class="text-sm text-amber-800">Login attempts are monitored and logged</span>
            </div>
          </div>
        </div>

        <!-- Login Button -->
        <button type="submit" class="admin-btn w-full text-white font-semibold py-4 rounded-xl relative">
          <span class="relative z-10">🚀 Access Admin Panel</span>
        </button>
      </form>

      <!-- Security Info -->
      <div class="mt-8 p-4 bg-gray-50 rounded-xl">
        <div class="text-center">
          <div class="text-sm text-gray-600 mb-2">🔒 Connection Secured</div>
          <div class="flex justify-center space-x-4 text-xs text-gray-500">
            <span>• 256-bit SSL</span>
            <span>• Activity Monitoring</span>
            <span>• IP Logging</span>
          </div>
        </div>
      </div>

      <!-- Back to User Login -->
      <div class="text-center mt-8 pt-6 border-t border-gray-200">
        <p class="text-sm text-gray-600">
          Not an admin?
          <a href="/login" class="text-indigo-600 hover:text-indigo-700 font-semibold hover:underline transition-colors ml-1">
            User Login
          </a>
        </p>
      </div>

      <!-- System Status -->
      <div class="mt-6 grid grid-cols-2 gap-4 text-center">
        <div class="bg-green-50 rounded-xl p-3">
          <div class="text-lg font-bold text-green-600">Online</div>
          <div class="text-xs text-gray-600">System Status</div>
        </div>
        <div class="bg-blue-50 rounded-xl p-3">
          <div class="text-lg font-bold text-blue-600">24/7</div>
          <div class="text-xs text-gray-600">Monitoring</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Mobile Security Notice -->
  <div class="lg:hidden fixed bottom-4 left-4 right-4 fade-in-delay-2">
    <div class="warning-box p-4 rounded-2xl text-white">
      <div class="flex items-center space-x-2 mb-2">
        <span class="text-xl">🔐</span>
        <span class="font-semibold">Security Notice</span>
      </div>
      <p class="text-sm text-white/90">
        Admin access is monitored. Ensure you're on a secure network before logging in.
      </p>
    </div>
  </div>

  <script>
    // Enhanced admin form validation and security features
    document.addEventListener('DOMContentLoaded', function() {
      const form = document.querySelector('form');
      const adminIdInput = document.getElementById('admin_id');
      const passwordInput = document.getElementById('password');
      const twoFaInput = document.getElementById('two_fa_code');
      
      // Admin ID validation
      adminIdInput.addEventListener('blur', function() {
        const adminId = this.value.trim();
        
        if (adminId.length >= 3) {
          this.style.borderColor = '#4338ca';
        } else if (adminId.length > 0) {
          this.style.borderColor = '#ef4444';
        }
      });
      
      // Password strength validation
      passwordInput.addEventListener('input', function() {
        const password = this.value;
        
        if (password.length >= 8) {
          this.style.borderColor = '#4338ca';
        } else if (password.length >= 6) {
          this.style.borderColor = '#f59e0b';
        } else if (password.length > 0) {
          this.style.borderColor = '#ef4444';
        }
      });
      
      // 2FA code formatting
      twoFaInput.addEventListener('input', function() {
        let value = this.value.replace(/\D/g, '');
        if (value.length <= 6) {
          this.value = value;
        }
        
        if (value.length === 6) {
          this.style.borderColor = '#4338ca';
        }
      });
      
      // Enhanced form submission with security checks
      form.addEventListener('submit', function(e) {
        const submitBtn = this.querySelector('button[type="submit"]');
        const originalText = submitBtn.querySelector('span').textContent;
        
        // Security validation
        const adminId = adminIdInput.value.trim();
        const password = passwordInput.value;
        
        if (adminId.length < 3) {
          alert('Admin ID must be at least 3 characters long');
          e.preventDefault();
          return;
        }
        
        if (password.length < 6) {
          alert('Password must be at least 6 characters long');
          e.preventDefault();
          return;
        }
        
        // Update button state
        submitBtn.querySelector('span').innerHTML = '🔐 Authenticating...';
        submitBtn.disabled = true;
        submitBtn.style.opacity = '0.7';
        
        // Log security attempt (in real app, this would be server-side)
        console.log('Admin login attempt:', {
          timestamp: new Date().toISOString(),
          adminId: adminId,
          userAgent: navigator.userAgent,
          ip: 'logged-server-side'
        });
        
        // Simulate authentication delay
        setTimeout(() => {
          submitBtn.querySelector('span').textContent = originalText;
          submitBtn.disabled = false;
          submitBtn.style.opacity = '1';
        }, 3000);
      });
      
      // Security monitoring - detect rapid multiple attempts
      let loginAttempts = 0;
      form.addEventListener('submit', function() {
        loginAttempts++;
        if (loginAttempts > 3) {
          console.warn('Multiple login attempts detected');
          // In real app, this would trigger security measures
        }
      });
      
      // Clear attempt counter after 15 minutes
      setTimeout(() => {
        loginAttempts = 0;
      }, 15 * 60 * 1000);
    });
  </script>
</body>
</html>